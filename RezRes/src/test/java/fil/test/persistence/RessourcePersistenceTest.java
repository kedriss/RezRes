/*
 * Created on 26 oct. 2015 ( Time 19:14:44 )
 * Generated by Telosys Tools Generator ( version 2.1.1 )
 */
package fil.test.persistence;


import org.junit.Assert;
import org.junit.Test;

import fil.bean.jpa.RessourceEntity;
import fil.mock.RessourceEntityMock;
import fil.persistence.PersistenceServiceProvider;
import fil.persistence.services.RessourcePersistence;

/**
 * JUnit test case for Ressource persistence service
 * 
 * @author Telosys Tools Generator
 *
 */
public class RessourcePersistenceTest 
{
	@Test
	public void test1() {
		
		System.out.println("Test count ..." );
		
		RessourcePersistence service = PersistenceServiceProvider.getService(RessourcePersistence.class);
		System.out.println("CountAll = " + service.countAll() );
	}
	
	@Test
	public void test2() {
		
		System.out.println("Test Ressource persistence : delete + load ..." );
		
		RessourcePersistence service = PersistenceServiceProvider.getService(RessourcePersistence.class);
		
		RessourceEntityMock mock = new RessourceEntityMock();
		
		// TODO : set primary key values here 
		process( service, mock, 0  );
		// process( service, mock, ... );
	}

	private void process(RessourcePersistence service, RessourceEntityMock mock, Integer id ) {
		System.out.println("----- "  );
		System.out.println(" . load : " );
		RessourceEntity entity = service.load( id );
		if ( entity != null ) {
			// Found 
			System.out.println("   FOUND : " + entity );
			
			// Save (update) with the same values to avoid database integrity errors  
			System.out.println(" . save : " + entity );
			service.save(entity);
			System.out.println("   saved : " + entity );
		}
		else {
			// Not found 
			System.out.println("   NOT FOUND" );
			// Create a new instance 
			entity = mock.createInstance( id ) ;
			Assert.assertNotNull(entity);

			// This entity references the following entities : 
			// . TypeRessource
			// . Utilisateur
			/* Insert only if references are OK
			// Try to insert the new instance
			System.out.println(" . insert : " + entity );
			service.insert(entity);
			System.out.println("   inserted : " + entity );
			*/

			System.out.println(" . delete : " );
			boolean deleted = service.delete( id );
			System.out.println("   deleted = " + deleted);
		}		
	}
}
